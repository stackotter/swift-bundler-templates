// swift-tools-version: 5.5

import PackageDescription

let package = Package(
	name: "{{PACKAGE}}",
	platforms: [.macOS(.v11), .iOS(.v15)],
	dependencies: [],
	targets: [
		.executableTarget(
			name: "{{PACKAGE}}",
			dependencies: [],
			resources: [.copy("Renderers/Shaders")]
		)
	]
)
