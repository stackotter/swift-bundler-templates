import SwiftUI

@main
struct {{TARGET}}: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
                .frame(initialWidth: 400, initialHeight: 200)
        }
    }
}