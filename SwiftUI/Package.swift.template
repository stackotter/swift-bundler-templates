// swift-tools-version: 5.5

import PackageDescription

let package = Package(
	name: "{{PACKAGE}}",
	platforms: [.macOS(.v11), .iOS(.v14)],
	dependencies: [],
	targets: [
		.executableTarget(
			name: "{{PACKAGE}}",
			dependencies: []
		)
	]
)
